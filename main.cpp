#include <iostream>
#include "pqueue.h"

int main() {
    persistent_queue pq {};

    // basic tests
    // pq.enqueue(4);
    // pq.enqueue(5);
    // pq.enqueue(7);
    // int elem = pq.dequeue();
    // std::cout << "Dequeued element: " << elem << " Size: " << pq.size() << '\n';
    // elem = pq.dequeue();
    // std::cout << "Dequeued element: " << elem << " Size: " << pq.size() << '\n';
    // elem = pq.dequeue();
    // std::cout << "Dequeued element: " << elem << " Size: " << pq.size() << '\n';
    // pq.enqueue(4);
    // std::cout << "Size: " << pq.size() << '\n';
}